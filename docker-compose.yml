version: "3"
services:
  web:
    build: ./hangboard-web
    ports: ['8080:8080']
  exercise:
    build: ./exercises
    ports: [4321:4321]

#  sensors:
#    build: ./hardware
    #devices:
    #  - /dev/gpiomem:/dev/gpiomem